---
const { title, subtitle, videoId = "8dfSzFQkV0s" } = Astro.props
---

<section class="relative py-20 lg:py-32 bg-gradient-to-b from-bs-surface-0 to-bs-surface-1">
  <div class="bs-container">
    
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      
      <!-- Left: Content -->
      <div class="space-y-6">
        <!-- <div class="inline-block px-4 py-2 bg-red-500/10 border border-red-500/20 rounded-full">
          <span class="text-red-400 font-semibold">Conoce MineTrack AI</span>
        </div> -->
        
        <h2 class="text-4xl md:text-5xl font-bold text-white leading-tight" set:html={title} />
        
        {subtitle && <p class="text-xl text-gray-300">{subtitle}</p>}

        <!-- <button 
          type="button"
          onclick="document.getElementById('video-modal').showModal()"
          class="group mt-6 px-8 py-4 bg-gradient-to-r from-red-500 to-red-600 rounded-xl font-semibold text-white shadow-lg shadow-red-500/30 hover:shadow-red-500/50 transition-all duration-300 hover:scale-105 inline-flex items-center gap-3"
        >
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
            <path d="M10 18a8 8 0 100-16 8 8 0 000 16zM9.555 7.168A1 1 0 008 8v4a1 1 0 001.555.832l3-2a1 1 0 000-1.664l-3-2z"/>
          </svg>
          Ver Video Demo
        </button> -->
      </div>

      <!-- Right: Video Preview -->
      <div class="relative flex justify-center">
        <div class="relative glass rounded-2xl overflow-hidden group cursor-pointer max-w-sm" onclick="document.getElementById('video-modal').showModal()">
          
          <!-- Video Thumbnail (Shorts - Vertical) -->
          <div class="aspect-[9/16] bg-gradient-to-br from-gray-800 to-gray-900 flex items-center justify-center relative overflow-hidden">

            <!-- YouTube Thumbnail -->
            <img 
              src={`https://img.youtube.com/vi/${videoId}/maxresdefault.jpg`}
              alt="Video preview"
              class="w-full h-full object-cover"
            />

            <!-- Play Button Overlay -->
            <div class="absolute inset-0 bg-black/30 flex items-center justify-center group-hover:bg-black/50 transition-all duration-300">
              <div class="w-20 h-20 bg-red-500 rounded-full flex items-center justify-center shadow-2xl shadow-red-500/50 group-hover:scale-110 transition-transform duration-300">
                <svg class="w-10 h-10 text-white ml-1" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M10 18a8 8 0 100-16 8 8 0 000 16zM9.555 7.168A1 1 0 008 8v4a1 1 0 001.555.832l3-2a1 1 0 000-1.664l-3-2z"/>
                </svg>
              </div>
            </div>

          </div>

          <!-- Decorative border -->
          <div class="absolute inset-0 border-2 border-red-500/20 rounded-2xl group-hover:border-red-500/40 transition-colors duration-300"></div>
        </div>

        <!-- Floating badge -->
        <!-- <div class="absolute -top-4 -right-4 bg-gradient-to-br from-emerald-500 to-emerald-600 text-white px-6 py-3 rounded-full shadow-xl shadow-emerald-500/30 font-semibold">
          <span class="flex items-center gap-2">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
              <path d="M2 6a2 2 0 012-2h6a2 2 0 012 2v8a2 2 0 01-2 2H4a2 2 0 01-2-2V6zM14.553 7.106A1 1 0 0014 8v4a1 1 0 00.553.894l2 1A1 1 0 0018 13V7a1 1 0 00-1.447-.894l-2 1z"/>
            </svg>
            Demo
          </span>
        </div> -->
      </div>

    </div>
  </div>

  <!-- Video Modal (Shorts - Vertical) -->
  <dialog id="video-modal" class="fixed inset-0 bg-black/90 m-auto z-50 p-0 rounded-2xl w-full max-w-md backdrop:bg-black/80">
    <div class="relative">
      <!-- Close Button -->
      <button 
        onclick="this.closest('dialog').close(); document.getElementById('video-iframe').src = document.getElementById('video-iframe').src"
        class="absolute -top-12 right-0 text-white hover:text-red-400 transition-colors duration-200 z-10"
        aria-label="Close"
      >
        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>

      <!-- Video iframe (Shorts) -->
      <div class="aspect-[9/16] w-full bg-black rounded-2xl overflow-hidden">
        <iframe 
          id="video-iframe"
          width="100%" 
          height="100%" 
          src={`https://www.youtube.com/embed/${videoId}?autoplay=0`}
          title="MineTrack AI App Demo"
          frameborder="0" 
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
          allowfullscreen
          class="w-full h-full"
        ></iframe>
      </div>
    </div>
  </dialog>
</section>

<style>
  dialog[open] {
    animation: fadeIn 0.3s ease-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: scale(0.95);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  dialog::backdrop {
    backdrop-filter: blur(8px);
  }
</style>
